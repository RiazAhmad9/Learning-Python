Learned a little about number types(float,int), basic calculation(sum,diff,prod,div,avg,rem), conversion(temperature,age), comparision(even or odd).
float is used for fraction numbers while integer is used for whole numbers.
To summarize, differentiate, production and diversion I have to use respectively +, -, *, / signs.
To convert temparature from celsius to fahrenheit I have to use the quation {c * 9 / 5 = 32} and to get a persons age I have to differemtiate the persons birth year from current year while the numbers are whole number.
To compare a number between odd and even I have to divide the number by 2 and check if theres any reminder with the command (number % 2 == 0).