Learned a little about string methods[strip,title,capitalize,split].
Strip() removes whitespaces from both end of a string, title() capitalizes the first letter of each word, capitalize() capitalizes the first word, split() breaks a string into parts.
To calculate in python I have to use number types such as float or int. If not then numbers will act as strings.
Learned about the function [round] which is used to round numbers to the nearest integer. But I can set it to a point of deecimal.
Learned a little about format specifier[:,] which separates the value with comma.
Learned how to define[def] my own function. [return] stores the value and gives back to the function to show the result.
