Started CS50's Introduction to Programming with Python Course.
Learned a little about variables, functions[print,input], string, argument, parameters[end,sep].
Variables work as containers that store values which I name as I wish.
Functions are block of codes which I can use any time. Input function is used to store text from user and print function is used to display output.
{} is used to put variable's value inside print and to embed these variable's I have to use [f] inside print.
String is any text inside quotes.
Argument is anything inside the functions parentheses[()] to run the function.
Parameters are used inside the function to define it. The default parameter end("") prints A new line at the end and default sep("") parameter prints space between values.