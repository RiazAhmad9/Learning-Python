Learned a little about conditional[if and else]. I can use it to check whether the value meet the condition or not.
Rather than useing [if/elif] I  can use the function [max()] to compare between numbers.
Learned hwo to fix(says invalid input) if user puts wrong input.